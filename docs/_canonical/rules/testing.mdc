---
description: Unit test policies, Vitest runner, gates, and co-location. Read when writing or reviewing *.test.ts, vitest config, or test infrastructure.
globs: "**/*.test.ts,**/*.test.tsx,vitest.config.ts,vitest.component.config.ts,vitest.integration.config.ts,stryker.conf.json,tests/**"
alwaysApply: false
---

# Testing Policy

## Strategy SSOT

Pyramid, waves, and module priorities: `@docs/05-plans/testing-strategy.md`.
Measured coverage numbers: `@docs/05-plans/testing-baseline.md`.
How-to: `@.cursor/skills/testing/SKILL.md`, patterns: `@.cursor/skills/testing/PATTERNS.md`.

## Runner and layout

- **Pin:** Vitest + `@vitest/coverage-v8` exact **`4.0.8`** (no `^`). Do **not** bump to 4.1.x without re-validating on Windows + Node 24 (`vi.mock`, forks, glob/dir entry).
- **Runner:** Vitest via wrappers where needed:
  - `npm run test` → `scripts/test-unit.mjs` (native cwd casing + hoisted vitest)
  - `npm run test:component` → `scripts/test-component.mjs` (explicit file list)
  - `npm run test:integration` → `scripts/test-integration.mjs` (explicit file list)
  - Also: `test:slow`, `test:contract`, `test:all`, `test:coverage`
- **Configs:** `@vitest.config.ts` (unit + coverage floors), `@vitest.slow.config.ts`, `@vitest.component.config.ts` (happy-dom, Preact JSX), `@vitest.integration.config.ts` (forks, **no** `setupFiles`)
- **File naming:** co-located `foo.test.ts` / `foo.test.tsx` next to source (not `.spec.ts`). Hook Testing Library suites may use `*.hook.test.ts`. Integration/contract/e2e live under `tests/`.
- **Imports:** NodeNext — use `.js` extensions in test imports (same as production code).
- **Wrappers allowed:** `scripts/test-*.mjs` / `scripts/resolve-vitest.mjs` for runner workarounds only — not for test cases. Put tests under `src/**/*.test.ts(x)` or `tests/`. Inventory helper `scripts/gen-test-inventory.mjs` is allowed.

## APP_SCOPE (unified)

Same scope for unit tests, coverage, and Stryker `mutate`:

- `src/**/*.ts` minus `*.test.ts` / `*.test.tsx` / `*.hook.test.ts`, `src/debug-app/**`, `src/prompt-lab-app/**`, `src/debug/**`, `src/prompt-lab/**`
- Includes **backend + client SPA**

SSOT: `@vitest.config.ts`, `@stryker.conf.json`. Baseline: `@docs/05-plans/testing-baseline.md`.

## Test environment

**Current state:** no dedicated test environment (no isolated Supabase / Redis / BullMQ stack for CI).

**Default (Q3):** mock external boundaries for unit, component, and mock-integration tests. Tests must not require prod/staging `.env` credentials.

| Term in Arcane (Q3) | Means |
|---------------------|-------|
| Unit test | co-located `*.test.ts`; `vi.mock` at DB/LLM/HTTP boundaries |
| Component test | `@testing-library/preact` + happy-dom; mocked `fetch` / hooks |
| Integration test (mock) | `createApp()` + supertest; mocked Supabase/Redis/LLM |
| Integration test (live) | **Q4 only** — real test stack |

**Q4 2026+ (blocked):** live integration + Playwright E2E on a dedicated test stack. Without test env, Wave 10 work is paused.

See `@docs/05-plans/testing-strategy.md`.

## Layer obligation (do not substitute)

| Situation | Layer / command |
|-----------|-----------------|
| Pure logic / Zod / handlers with mocks | Co-located unit (`npm run test`) |
| Preact UI / hooks | `npm run test:component` (`*.test.tsx` / `*.hook.test.ts`) |
| Presentational markup (`ui/*`, EntityCard, TagChip) | `toMatchSnapshot` in component suite — update `.snap` intentionally in the same PR |
| HTTP middleware order / status wiring / job runner | `tests/integration/**` mock-first (`npm run test:integration`) |
| Bug fix in pure logic (`engine/`, `shared/`, utils) | Regression in co-located `*.test.ts` |
| Live LLM / OpenAI API | **Never** in unit/component/mock-integration — mock provider |
| Live Supabase / Redis / BullMQ | **Q4 only** on dedicated test environment |

Do **not** use unit tests as a substitute for mock-integration wiring checks, or vice versa. Do **not** snapshot full pages, `ReadingMode`, Auth gates, or async-loaded DOM.

## Test types in Arcane

| Type | Q3 | Q4 (test env required) |
|------|-----|------------------------|
| Unit | pure logic, co-located, mocked boundaries | same |
| Component | Testing Library + mocked `fetch` | optional live DOM + test API |
| Integration (mock) | `createApp` + supertest + mocked services | keep as fast CI layer |
| Integration (live) | — | real Supabase / Redis / worker |
| Snapshot | Presentational `ui/*` / EntityCard / TagChip (`toMatchSnapshot`); no breakpoint matrix | same |
| Contract | Phase 1 Zod fixtures under `tests/contracts/` (pre-push) | OpenAPI / Pact after split |
| System/E2E | stub only | Playwright smoke on test stack |

## Layer boundaries

| Layer | Allowed in tests (Q3) | Forbidden |
|-------|----------------------|-----------|
| `src/engine/**` | Pure pipeline, glossary, prompts, mocked LLM | HTTP, Supabase, `req.log` |
| `src/shared/**` | Pure utilities, business rules | DB, Redis |
| `src/api/**` | `validateRoute` helpers, Zod; mocked middleware | Full Express app without mocks (use `createApp` + mocks in `tests/integration`) |
| `src/client/**` | URL builders, hooks, components with mocked `fetch` | Live API without mocks |
| `src/services/**` | extracted pure helpers with mocks | live `supabaseDatabase` calls |
| `src/createApp.ts` | mock-integration via supertest | listen() in tests |

## Mutation testing (manual)

- Command: `npm run test:mutation` or zone smoke `npx stryker run --mutate <glob>`
- Scope: APP_SCOPE (same as coverage)
- **Not** in CI or pre-push; manual/nightly only
- `thresholds.high: 80`, `low: 60`, **`break: null`** — advisory bands / trend only, not a merge gate
- Report: `reports/mutation/mutation.html`

## Gates

- Before **push:** `npm run lint:all`, `npm run test`, `npm run test:component`, `npm run test:integration`, and `npm run test:contract` (enforced via `.husky/pre-push`).
- Before **commit:** pre-commit runs lint-staged only (fast).
- **Coverage floors:** `npm run test:coverage` enforces `coverage.thresholds` in `@vitest.config.ts` (lines **64**, branches **54** — measured floors). **Not** in pre-push.
- If a PR drops below floors, fix tests or adjust floors **deliberately** in the same PR (never silent lower).

## Coverage policy

- Floors on `test:coverage` only; track trend above floors.
- Prioritize `engine/` and `shared/` pure modules; expand to client components/hooks and mock-integration.
- Baseline: `@docs/05-plans/testing-baseline.md`. Strategy: `@docs/05-plans/testing-strategy.md`.

## Documentation workflow

When changing runner, gates, or test conventions:

1. Update this rule (`testing.mdc`) and `@.cursor/skills/testing/SKILL.md`.
2. Mirror to `@docs/_canonical/rules/testing.mdc`.
3. Update `@docs/05-plans/testing-strategy.md` / baseline if waves or scripts change.
4. Update `@AGENTS.md` commands table if scripts change.

## Agent routing

- Test infrastructure, migration, coverage baseline → **Testing** utility agent (`@.cursor/agents/testing/AGENT.md`).
- Feature code + tests → domain agent (Engine/API/Backend/UI) + **verifier** runs `lint:all` + `test` (+ component/integration when those layers change).
