---
stale: true
status: archived
domain: meta
---

# Supabase Edge Functions

## 1. РђРІС‚РѕРѕС‡РёСЃС‚РєР° СЌРєСЃРїРѕСЂС‚РѕРІ (`cleanup-exports`)

Р¦РµР»СЊ: РЅРµ Р·Р°Р±РёРІР°С‚СЊ Storage (Сѓ РЅР°СЃ Р»РёРјРёС‚ 1 GB) вЂ” Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё СѓРґР°Р»СЏС‚СЊ СЃС‚Р°СЂС‹Рµ С„Р°Р№Р»С‹ РёР· bucket `exports`.

Р”РѕР±Р°РІР»РµРЅР° edge function `cleanup-exports`:

- **РџР°РїРєР°**: `supabase/functions/cleanup-exports/index.ts`
- **Bucket**: `exports`
- **РЎС‚СЂР°С‚РµРіРёСЏ**:
  - СѓРґР°Р»РёС‚СЊ С„Р°Р№Р»С‹ СЃС‚Р°СЂС€Рµ \(N\) РґРЅРµР№
  - Рё/РёР»Рё РѕСЃС‚Р°РІРёС‚СЊ С‚РѕР»СЊРєРѕ РїРѕСЃР»РµРґРЅРёРµ \(M\) С„Р°Р№Р»РѕРІ РІ РєР°Р¶РґРѕРј РїСЂРѕРµРєС‚Рµ (РїР°РїРєР° `exports/{projectId}/`)

## РџРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ (Supabase в†’ Project Settings в†’ Functions)

РћР±СЏР·Р°С‚РµР»СЊРЅС‹Рµ:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` (**СЃРµРєСЂРµС‚**, С‚РѕР»СЊРєРѕ РґР»СЏ СЃРµСЂРІРµСЂРЅРѕРіРѕ РєРѕРґР°)

РћРїС†РёРѕРЅР°Р»СЊРЅС‹Рµ:

- `EXPORTS_RETENTION_DAYS` (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `7`)
- `EXPORTS_KEEP_LATEST` (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `5`)
- `EXPORTS_BUCKET` (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `exports`)
- `EXPORTS_ROOT_PREFIX` (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ РїСѓСЃС‚Рѕ) вЂ” РµСЃР»Рё РІС‹ С…СЂР°РЅРёС‚Рµ СЌРєСЃРїРѕСЂС‚С‹ РЅРµ РІ РєРѕСЂРЅРµ bucket'Р°

## Р”РµРїР»РѕР№ Рё СЂР°СЃРїРёСЃР°РЅРёРµ

### 1) Р”РµРїР»РѕР№ С„СѓРЅРєС†РёРё

```bash
supabase functions deploy cleanup-exports --no-verify-jwt
```

`--no-verify-jwt` СѓРґРѕР±РµРЅ РґР»СЏ cron-РІС‹Р·РѕРІР° (РёРЅР°С‡Рµ РЅСѓР¶РµРЅ JWT).

### 2) РЎРѕР·РґР°РЅРёРµ СЂР°СЃРїРёСЃР°РЅРёСЏ (cron)

РџСЂРёРјРµСЂ: Р·Р°РїСѓСЃРє РєР°Р¶РґС‹Р№ РґРµРЅСЊ РІ 03:15 UTC:

```bash
supabase functions schedule create cleanup-exports \
  --cron "15 3 * * *"
```

РџРѕСЃРјРѕС‚СЂРµС‚СЊ СЂР°СЃРїРёСЃР°РЅРёСЏ:

```bash
supabase functions schedule list
```

РЈРґР°Р»РёС‚СЊ СЂР°СЃРїРёСЃР°РЅРёРµ:

```bash
supabase functions schedule delete <schedule-id>
```

## Р СѓС‡РЅРѕР№ Р·Р°РїСѓСЃРє (РґР»СЏ РїСЂРѕРІРµСЂРєРё)

РњРѕР¶РЅРѕ РІС‹Р·РІР°С‚СЊ HTTP endpoint С„СѓРЅРєС†РёРё (URL Р±РµСЂС‘С‚СЃСЏ РёР· Supabase Dashboard в†’ Edge Functions в†’ `cleanup-exports`).

РњРµС‚РѕРґС‹: `GET` РёР»Рё `POST`.

РћС‚РІРµС‚ вЂ” JSON СЃРѕ СЃС‚Р°С‚РёСЃС‚РёРєРѕР№ СѓРґР°Р»РµРЅРёР№ Рё РѕС€РёР±РєР°РјРё, РµСЃР»Рё РѕРЅРё Р±С‹Р»Рё.

## РџСЂРёРјРµС‡Р°РЅРёСЏ

- Bucket `exports` Р»СѓС‡С€Рµ РґРµСЂР¶Р°С‚СЊ **private** (СЃРєР°С‡РёРІР°РЅРёРµ РґРµР»Р°С‚СЊ С‡РµСЂРµР· signed URL).
- РћС‡РёСЃС‚РєР° РЅРµ РґРѕР»Р¶РЅР° Р»РѕРјР°С‚СЊ СЌРєСЃРїРѕСЂС‚: СЌС‚Рѕ РѕС‚РґРµР»СЊРЅС‹Р№ РїРµСЂРёРѕРґРёС‡РµСЃРєРёР№ РїСЂРѕС†РµСЃСЃ.

---

## 2. РЎР±СЂРѕСЃ РґРЅРµРІРЅС‹С… Р»РёРјРёС‚РѕРІ С‚РѕРєРµРЅРѕРІ (`reset-daily-token-limits`)

Р¦РµР»СЊ: РѕС‡РёСЃС‚РєР° СЃС‚Р°СЂС‹С… Р·Р°РїРёСЃРµР№ РёСЃРїРѕР»СЊР·РѕРІР°РЅРёСЏ С‚РѕРєРµРЅРѕРІ РёР· С‚Р°Р±Р»РёС†С‹ `user_token_usage` РґР»СЏ РїСЂРµРґРѕС‚РІСЂР°С‰РµРЅРёСЏ СЂР°Р·СЂР°СЃС‚Р°РЅРёСЏ Р±Р°Р·С‹ РґР°РЅРЅС‹С….

Р”РѕР±Р°РІР»РµРЅР° edge function `reset-daily-token-limits`:

- **РџР°РїРєР°**: `supabase/functions/reset-daily-token-limits/index.ts`
- **РўР°Р±Р»РёС†Р°**: `user_token_usage`
- **РЎС‚СЂР°С‚РµРіРёСЏ**:
  - СѓРґР°Р»РёС‚СЊ Р·Р°РїРёСЃРё СЃС‚Р°СЂС€Рµ N РґРЅРµР№ (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ 30 РґРЅРµР№)
  - СЃРѕС…СЂР°РЅСЏРµС‚ С‚РѕР»СЊРєРѕ Р°РєС‚СѓР°Р»СЊРЅС‹Рµ РґР°РЅРЅС‹Рµ РґР»СЏ СЃС‚Р°С‚РёСЃС‚РёРєРё

### РџРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ (Supabase в†’ Project Settings в†’ Functions)

РћР±СЏР·Р°С‚РµР»СЊРЅС‹Рµ:

- `SUPABASE_URL`
- `SUPABASE_SERVICE_ROLE_KEY` (**СЃРµРєСЂРµС‚**, С‚РѕР»СЊРєРѕ РґР»СЏ СЃРµСЂРІРµСЂРЅРѕРіРѕ РєРѕРґР°)

РћРїС†РёРѕРЅР°Р»СЊРЅС‹Рµ:

- `TOKEN_RETENTION_DAYS` (РїРѕ СѓРјРѕР»С‡Р°РЅРёСЋ `30`)

### Р”РµРїР»РѕР№ Рё СЂР°СЃРїРёСЃР°РЅРёРµ

#### 1) Р”РµРїР»РѕР№ С„СѓРЅРєС†РёРё

```bash
supabase functions deploy reset-daily-token-limits --no-verify-jwt
```

`--no-verify-jwt` СѓРґРѕР±РµРЅ РґР»СЏ cron-РІС‹Р·РѕРІР° (РёРЅР°С‡Рµ РЅСѓР¶РµРЅ JWT).

#### 2) РЎРѕР·РґР°РЅРёРµ СЂР°СЃРїРёСЃР°РЅРёСЏ (cron)

РџСЂРёРјРµСЂ: Р·Р°РїСѓСЃРє РєР°Р¶РґС‹Р№ РґРµРЅСЊ РІ 00:00 UTC (РІ РїРѕР»РЅРѕС‡СЊ):

```bash
supabase functions schedule create reset-daily-token-limits \
  --cron "0 0 * * *"
```

РџРѕСЃРјРѕС‚СЂРµС‚СЊ СЂР°СЃРїРёСЃР°РЅРёСЏ:

```bash
supabase functions schedule list
```

РЈРґР°Р»РёС‚СЊ СЂР°СЃРїРёСЃР°РЅРёРµ:

```bash
supabase functions schedule delete <schedule-id>
```

### Р СѓС‡РЅРѕР№ Р·Р°РїСѓСЃРє (РґР»СЏ РїСЂРѕРІРµСЂРєРё)

РњРѕР¶РЅРѕ РІС‹Р·РІР°С‚СЊ HTTP endpoint С„СѓРЅРєС†РёРё (URL Р±РµСЂС‘С‚СЃСЏ РёР· Supabase Dashboard в†’ Edge Functions в†’ `reset-daily-token-limits`).

РњРµС‚РѕРґС‹: `GET` РёР»Рё `POST`.

РћС‚РІРµС‚ вЂ” JSON СЃРѕ СЃС‚Р°С‚РёСЃС‚РёРєРѕР№ СѓРґР°Р»РµРЅРёР№:

```json
{
  "success": true,
  "deletedCount": 150,
  "oldestDate": "2025-12-01",
  "newestDate": "2026-01-28",
  "cutoffDate": "2025-12-30",
  "retentionDays": 30
}
```

### РџСЂРёРјРµС‡Р°РЅРёСЏ

- Р¤СѓРЅРєС†РёСЏ СѓРґР°Р»СЏРµС‚ С‚РѕР»СЊРєРѕ СЃС‚Р°СЂС‹Рµ Р·Р°РїРёСЃРё, РЅРµ РІР»РёСЏСЏ РЅР° С‚РµРєСѓС‰РёРµ РґРЅРµРІРЅС‹Рµ Р»РёРјРёС‚С‹.
- РќРѕРІС‹Рµ Р·Р°РїРёСЃРё СЃРѕР·РґР°СЋС‚СЃСЏ Р°РІС‚РѕРјР°С‚РёС‡РµСЃРєРё РїСЂРё РїРµСЂРІРѕРј РёСЃРїРѕР»СЊР·РѕРІР°РЅРёРё С‚РѕРєРµРЅРѕРІ РїРѕР»СЊР·РѕРІР°С‚РµР»РµРј.
- Р РµРєРѕРјРµРЅРґСѓРµС‚СЃСЏ Р·Р°РїСѓСЃРєР°С‚СЊ РІ РїРѕР»РЅРѕС‡СЊ UTC РґР»СЏ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё СЃ РґРЅРµРІРЅС‹РјРё Р»РёРјРёС‚Р°РјРё.
