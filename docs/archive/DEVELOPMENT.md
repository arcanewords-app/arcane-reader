---
stale: true
status: archived
domain: meta
---

# Р СѓРєРѕРІРѕРґСЃС‚РІРѕ РїРѕ СЂР°Р·СЂР°Р±РѕС‚РєРµ

## РќР°СЃС‚СЂРѕР№РєР° РѕРєСЂСѓР¶РµРЅРёСЏ СЂР°Р·СЂР°Р±РѕС‚РєРё

### РўСЂРµР±РѕРІР°РЅРёСЏ

- Node.js 20.x РёР»Рё РІС‹С€Рµ
- npm РёР»Рё yarn
- Git
- Р РµРґР°РєС‚РѕСЂ РєРѕРґР° (VS Code СЂРµРєРѕРјРµРЅРґСѓРµС‚СЃСЏ)

### РЈСЃС‚Р°РЅРѕРІРєР°

1. **РљР»РѕРЅРёСЂСѓР№С‚Рµ СЂРµРїРѕР·РёС‚РѕСЂРёР№**:
```bash
git clone <repository-url>
cd arcane-reader
```

2. **РЈСЃС‚Р°РЅРѕРІРёС‚Рµ Р·Р°РІРёСЃРёРјРѕСЃС‚Рё**:
```bash
npm install
```

3. **РќР°СЃС‚СЂРѕР№С‚Рµ РїРµСЂРµРјРµРЅРЅС‹Рµ РѕРєСЂСѓР¶РµРЅРёСЏ**:
```bash
cp env.example.txt .env
# РћС‚СЂРµРґР°РєС‚РёСЂСѓР№С‚Рµ .env СЃ РІР°С€РёРјРё РєР»СЋС‡Р°РјРё
```

4. **Р—Р°РїСѓСЃС‚РёС‚Рµ dev-СЃРµСЂРІРµСЂ**:
```bash
npm run dev
```

Р­С‚Рѕ Р·Р°РїСѓСЃС‚РёС‚:
- Express СЃРµСЂРІРµСЂ РЅР° `http://localhost:3000`
- Vite dev СЃРµСЂРІРµСЂ РЅР° `http://localhost:5173`

## РЎС‚СЂСѓРєС‚СѓСЂР° РїСЂРѕРµРєС‚Р°

```
src/
в”њв”Ђв”Ђ client/              # Frontend (Preact)
в”‚   в”њв”Ђв”Ђ api/            # API РєР»РёРµРЅС‚
в”‚   в”њв”Ђв”Ђ components/     # UI РєРѕРјРїРѕРЅРµРЅС‚С‹
в”‚   в”њв”Ђв”Ђ pages/          # РЎС‚СЂР°РЅРёС†С‹ (СЂРѕСѓС‚РёРЅРі)
в”‚   в”њв”Ђв”Ђ services/       # РљР»РёРµРЅС‚СЃРєРёРµ СЃРµСЂРІРёСЃС‹
в”‚   в”њв”Ђв”Ђ types/          # TypeScript С‚РёРїС‹
в”‚   в””в”Ђв”Ђ utils/          # РЈС‚РёР»РёС‚С‹
в”њв”Ђв”Ђ engine/              # Р”РІРёР¶РѕРє РїРµСЂРµРІРѕРґР°
в”‚   в”њв”Ђв”Ђ agents/         # AI Р°РіРµРЅС‚С‹
в”‚   в”њв”Ђв”Ђ stages/         # РЎС‚Р°РґРёРё pipeline
в”‚   в”њв”Ђв”Ђ providers/      # LLM РїСЂРѕРІР°Р№РґРµСЂС‹
в”‚   в””в”Ђв”Ђ types/          # РўРёРїС‹ РґРІРёР¶РєР°
в”њв”Ђв”Ђ middleware/          # Express middleware
в”њв”Ђв”Ђ services/            # Р‘РёР·РЅРµСЃ-Р»РѕРіРёРєР°
в”‚   в”њв”Ђв”Ђ export/         # Р­РєСЃРїРѕСЂС‚ EPUB/FB2
в”‚   в”њв”Ђв”Ђ import/         # РРјРїРѕСЂС‚ С„Р°Р№Р»РѕРІ
в”‚   в””в”Ђв”Ђ storage.ts      # Supabase Storage
в”њв”Ђв”Ђ storage/             # РЎР»РѕР№ Р±Р°Р·С‹ РґР°РЅРЅС‹С…
в”њв”Ђв”Ђ utils/               # РЈС‚РёР»РёС‚С‹ СЃРµСЂРІРµСЂР°
в””в”Ђв”Ђ server.ts           # Express СЃРµСЂРІРµСЂ
```

## РЎРєСЂРёРїС‚С‹ СЂР°Р·СЂР°Р±РѕС‚РєРё

### РћСЃРЅРѕРІРЅС‹Рµ РєРѕРјР°РЅРґС‹

```bash
# Р Р°Р·СЂР°Р±РѕС‚РєР° (Р·Р°РїСѓСЃРєР°РµС‚ РѕР±Р° СЃРµСЂРІРµСЂР°)
npm run dev

# РўРѕР»СЊРєРѕ СЃРµСЂРІРµСЂ
npm run dev:server

# РўРѕР»СЊРєРѕ РєР»РёРµРЅС‚
npm run dev:client

# РЎР±РѕСЂРєР°
npm run build

# РЎР±РѕСЂРєР° РєР»РёРµРЅС‚Р°
npm run build:client

# РЎР±РѕСЂРєР° СЃРµСЂРІРµСЂР°
npm run build:server

# Р—Р°РїСѓСЃРє production СЃРµСЂРІРµСЂР°
npm start

# Р›РёРЅС‚РёРЅРі
npm run lint

# РЈР±РёС‚СЊ РїСЂРѕС†РµСЃСЃ РЅР° РїРѕСЂС‚Сѓ 3000
npm run kill-port
```

## Р Р°Р±РѕС‚Р° СЃ РєРѕРґРѕРј

### TypeScript

РџСЂРѕРµРєС‚ РёСЃРїРѕР»СЊР·СѓРµС‚ TypeScript РґР»СЏ С‚РёРїРёР·Р°С†РёРё.

**Backend С‚РёРїС‹**: `src/storage/database.ts`, `src/types/`
**Frontend С‚РёРїС‹**: `src/client/types/index.ts`
**Engine С‚РёРїС‹**: `src/engine/types/`

### РЎС‚РёР»СЊ РєРѕРґР°

- РСЃРїРѕР»СЊР·СѓР№С‚Рµ ESLint РґР»СЏ РїСЂРѕРІРµСЂРєРё СЃС‚РёР»СЏ
- Р¤РѕСЂРјР°С‚РёСЂРѕРІР°РЅРёРµ С‡РµСЂРµР· Prettier (РµСЃР»Рё РЅР°СЃС‚СЂРѕРµРЅ)
- РЎР»РµРґСѓР№С‚Рµ СЃСѓС‰РµСЃС‚РІСѓСЋС‰РёРј РїР°С‚С‚РµСЂРЅР°Рј РІ РєРѕРґРµ

### РљРѕРјРјРёС‚С‹

РСЃРїРѕР»СЊР·СѓР№С‚Рµ РїРѕРЅСЏС‚РЅС‹Рµ СЃРѕРѕР±С‰РµРЅРёСЏ РєРѕРјРјРёС‚РѕРІ:
```
feat: РґРѕР±Р°РІРёС‚СЊ СЌРєСЃРїРѕСЂС‚ РІ FB2
fix: РёСЃРїСЂР°РІРёС‚СЊ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёСЋ РїР°СЂР°РіСЂР°С„РѕРІ
docs: РѕР±РЅРѕРІРёС‚СЊ РґРѕРєСѓРјРµРЅС‚Р°С†РёСЋ API
refactor: СЂРµС„Р°РєС‚РѕСЂРёРЅРі РґРІРёР¶РєР° РїРµСЂРµРІРѕРґР°
```

## Р”РѕР±Р°РІР»РµРЅРёРµ РЅРѕРІС‹С… С„СѓРЅРєС†РёР№

### Р”РѕР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕРіРѕ API endpoint

1. **Р”РѕР±Р°РІСЊС‚Рµ route РІ `src/server.ts`**:
```typescript
app.get('/api/new-endpoint', requireAuth, async (req, res) => {
  // Р’Р°С€ РєРѕРґ
});
```

2. **Р”РѕР±Р°РІСЊС‚Рµ РјРµС‚РѕРґ РІ API РєР»РёРµРЅС‚** (`src/client/api/client.ts`):
```typescript
async newMethod(): Promise<ResponseType> {
  return this.request('/api/new-endpoint', { method: 'GET' });
}
```

3. **Р”РѕР±Р°РІСЊС‚Рµ С‚РёРїС‹** (РµСЃР»Рё РЅСѓР¶РЅРѕ):
```typescript
// src/client/types/index.ts
export interface NewType {
  // ...
}
```

### Р”РѕР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕРіРѕ РєРѕРјРїРѕРЅРµРЅС‚Р°

1. **РЎРѕР·РґР°Р№С‚Рµ РєРѕРјРїРѕРЅРµРЅС‚** РІ `src/client/components/`:
```typescript
// src/client/components/NewComponent.tsx
export function NewComponent({ prop }: Props) {
  return <div>...</div>;
}
```

2. **РСЃРїРѕР»СЊР·СѓР№С‚Рµ РєРѕРјРїРѕРЅРµРЅС‚**:
```typescript
import { NewComponent } from './components/NewComponent';
```

### Р”РѕР±Р°РІР»РµРЅРёРµ РЅРѕРІРѕР№ СЃС‚Р°РґРёРё РІ pipeline

1. **РЎРѕР·РґР°Р№С‚Рµ РєР»Р°СЃСЃ СЃС‚Р°РґРёРё** РІ `src/engine/stages/`:
```typescript
export class NewStage {
  async execute(text: string, options: StageOptions): Promise<StageResult> {
    // Р’Р°С€Р° Р»РѕРіРёРєР°
  }
}
```

2. **Р”РѕР±Р°РІСЊС‚Рµ РІ pipeline** (`src/engine/pipeline/translation-pipeline.ts`):
```typescript
private newStage: NewStage;

constructor(config: PipelineConfig) {
  this.newStage = new NewStage(config.providers.newStage);
}
```

3. **Р’С‹Р·РѕРІРёС‚Рµ РІ РјРµС‚РѕРґРµ translateChapter**:
```typescript
const newStageResult = await this.newStage.execute(text, options);
```

## РўРµСЃС‚РёСЂРѕРІР°РЅРёРµ

### Р СѓС‡РЅРѕРµ С‚РµСЃС‚РёСЂРѕРІР°РЅРёРµ

1. **Р—Р°РїСѓСЃС‚РёС‚Рµ dev-СЃРµСЂРІРµСЂ**:
```bash
npm run dev
```

2. **РћС‚РєСЂРѕР№С‚Рµ Р±СЂР°СѓР·РµСЂ**: `http://localhost:5173`

3. **РџСЂРѕРІРµСЂСЊС‚Рµ С„СѓРЅРєС†РёРѕРЅР°Р»СЊРЅРѕСЃС‚СЊ**:
   - РЎРѕР·РґР°РЅРёРµ РїСЂРѕРµРєС‚Р°
   - Р—Р°РіСЂСѓР·РєР° РіР»Р°РІС‹
   - РџРµСЂРµРІРѕРґ
   - Р РµРґР°РєС‚РёСЂРѕРІР°РЅРёРµ
   - Р­РєСЃРїРѕСЂС‚

### РўРµСЃС‚РёСЂРѕРІР°РЅРёРµ API

РСЃРїРѕР»СЊР·СѓР№С‚Рµ curl РёР»Рё Postman РґР»СЏ С‚РµСЃС‚РёСЂРѕРІР°РЅРёСЏ API:

```bash
# РџРѕР»СѓС‡РёС‚СЊ СЃС‚Р°С‚СѓСЃ
curl http://localhost:3000/api/status

# Р’РѕР№С‚Рё
curl -X POST http://localhost:3000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"password"}'
```

## РћС‚Р»Р°РґРєР°

### Backend РѕС‚Р»Р°РґРєР°

РСЃРїРѕР»СЊР·СѓР№С‚Рµ `console.log` РґР»СЏ Р»РѕРіРёСЂРѕРІР°РЅРёСЏ:

```typescript
console.log('Debug info:', data);
```

Р›РѕРіРё РІС‹РІРѕРґСЏС‚СЃСЏ РІ РєРѕРЅСЃРѕР»СЊ СЃРµСЂРІРµСЂР°.

### Frontend РѕС‚Р»Р°РґРєР°

РСЃРїРѕР»СЊР·СѓР№С‚Рµ DevTools Р±СЂР°СѓР·РµСЂР°:
- **Console** вЂ” РґР»СЏ Р»РѕРіРѕРІ
- **Network** вЂ” РґР»СЏ Р·Р°РїСЂРѕСЃРѕРІ API
- **React DevTools** вЂ” РґР»СЏ РєРѕРјРїРѕРЅРµРЅС‚РѕРІ (Preact СЃРѕРІРјРµСЃС‚РёРј)

### РћС‚Р»Р°РґРєР° РґРІРёР¶РєР° РїРµСЂРµРІРѕРґР°

Р›РѕРіРё РґРІРёР¶РєР° РІС‹РІРѕРґСЏС‚СЃСЏ СЃ РїСЂРµС„РёРєСЃР°РјРё:
- `[Pipeline]` вЂ” Р»РѕРіРё pipeline
- `[Stage 1]` вЂ” Р»РѕРіРё СЃС‚Р°РґРёРё Р°РЅР°Р»РёР·Р°
- `[Stage 2]` вЂ” Р»РѕРіРё СЃС‚Р°РґРёРё РїРµСЂРµРІРѕРґР°
- `[Stage 3]` вЂ” Р»РѕРіРё СЃС‚Р°РґРёРё СЂРµРґР°РєС‚СѓСЂС‹

## Р Р°Р±РѕС‚Р° СЃ Р±Р°Р·РѕР№ РґР°РЅРЅС‹С…

### Р›РѕРєР°Р»СЊРЅР°СЏ СЂР°Р·СЂР°Р±РѕС‚РєР°

Р”Р»СЏ Р»РѕРєР°Р»СЊРЅРѕР№ СЂР°Р·СЂР°Р±РѕС‚РєРё РёСЃРїРѕР»СЊР·СѓР№С‚Рµ Supabase Р»РѕРєР°Р»СЊРЅРѕ РёР»Рё РїРѕРґРєР»СЋС‡РёС‚РµСЃСЊ Рє dev РїСЂРѕРµРєС‚Сѓ.

### РњРёРіСЂР°С†РёРё

РџСЂРё РёР·РјРµРЅРµРЅРёРё СЃС…РµРјС‹ Р±Р°Р·С‹ РґР°РЅРЅС‹С…:

1. РЎРѕР·РґР°Р№С‚Рµ SQL РјРёРіСЂР°С†РёСЋ
2. РџСЂРёРјРµРЅРёС‚Рµ С‡РµСЂРµР· Supabase Dashboard в†’ **SQL Editor**
3. РћР±РЅРѕРІРёС‚Рµ С‚РёРїС‹ РІ `src/storage/database.ts` РµСЃР»Рё РЅСѓР¶РЅРѕ

### РџСЂРѕСЃРјРѕС‚СЂ РґР°РЅРЅС‹С…

РСЃРїРѕР»СЊР·СѓР№С‚Рµ Supabase Dashboard в†’ **Table Editor** РґР»СЏ РїСЂРѕСЃРјРѕС‚СЂР° Рё СЂРµРґР°РєС‚РёСЂРѕРІР°РЅРёСЏ РґР°РЅРЅС‹С….

## Р Р°Р±РѕС‚Р° СЃ Supabase Storage

### Р—Р°РіСЂСѓР·РєР° С„Р°Р№Р»РѕРІ

```typescript
import { uploadFile } from './services/storage';

const result = await uploadFile(
  'images',
  'path/to/file.jpg',
  buffer,
  { contentType: 'image/jpeg' }
);
```

### РџРѕР»СѓС‡РµРЅРёРµ РїСѓР±Р»РёС‡РЅРѕРіРѕ URL

```typescript
import { getPublicUrl } from './services/storage';

const url = getPublicUrl('images', 'path/to/file.jpg');
```

## РџСЂРѕРёР·РІРѕРґРёС‚РµР»СЊРЅРѕСЃС‚СЊ

### РћРїС‚РёРјРёР·Р°С†РёСЏ РїРµСЂРµРІРѕРґРѕРІ

- РСЃРїРѕР»СЊР·СѓР№С‚Рµ Р±РѕР»РµРµ РґРµС€РµРІС‹Рµ РјРѕРґРµР»Рё РґР»СЏ Р°РЅР°Р»РёР·Р°/СЂРµРґР°РєС‚СѓСЂС‹
- РќР°СЃС‚СЂРѕР№С‚Рµ `MAX_TOKENS_PER_CHUNK` РґР»СЏ Р±Р°Р»Р°РЅСЃР° СЃРєРѕСЂРѕСЃС‚Рё/РєР°С‡РµСЃС‚РІР°
- РСЃРїРѕР»СЊР·СѓР№С‚Рµ `SKIP_EDITING=true` РґР»СЏ Р±С‹СЃС‚СЂС‹С… РїРµСЂРµРІРѕРґРѕРІ

### РћРїС‚РёРјРёР·Р°С†РёСЏ С„СЂРѕРЅС‚РµРЅРґР°

- РСЃРїРѕР»СЊР·СѓР№С‚Рµ lazy loading РґР»СЏ РєРѕРјРїРѕРЅРµРЅС‚РѕРІ
- РћРїС‚РёРјРёР·РёСЂСѓР№С‚Рµ СЂРµ-СЂРµРЅРґРµСЂС‹ С‡РµСЂРµР· `useMemo` Рё `useCallback`
- РњРёРЅРёРјРёР·РёСЂСѓР№С‚Рµ СЂР°Р·РјРµСЂ bundle

## РР·РІРµСЃС‚РЅС‹Рµ РїСЂРѕР±Р»РµРјС‹

### РџСЂРѕР±Р»РµРјР°: РџРµСЂРµРІРѕРґ Р·Р°СЃС‚СЂРµРІР°РµС‚ РІ СЃС‚Р°С‚СѓСЃРµ "translating"

**Р РµС€РµРЅРёРµ**: РСЃРїРѕР»СЊР·СѓР№С‚Рµ endpoint `/api/projects/:id/chapters/:id/translate/cancel` РґР»СЏ СЃР±СЂРѕСЃР° СЃС‚Р°С‚СѓСЃР°.

### РџСЂРѕР±Р»РµРјР°: РџР°СЂР°РіСЂР°С„С‹ РЅРµ СЃРёРЅС…СЂРѕРЅРёР·РёСЂСѓСЋС‚СЃСЏ СЃ РїРµСЂРµРІРѕРґРѕРј

**Р РµС€РµРЅРёРµ**: РСЃРїРѕР»СЊР·СѓР№С‚Рµ endpoint `/api/projects/:id/chapters/:id/translate/sync` РґР»СЏ СЂСѓС‡РЅРѕР№ СЃРёРЅС…СЂРѕРЅРёР·Р°С†РёРё.

### РџСЂРѕР±Р»РµРјР°: РћС€РёР±РєР° РїСЂРё Р·Р°РіСЂСѓР·РєРµ EPUB/FB2

**Р РµС€РµРЅРёРµ**: РџСЂРѕРІРµСЂСЊС‚Рµ С„РѕСЂРјР°С‚ С„Р°Р№Р»Р° Рё СЂР°Р·РјРµСЂ (Р»РёРјРёС‚ 50MB).

## РџРѕР»РµР·РЅС‹Рµ СЂРµСЃСѓСЂСЃС‹

- [Preact РґРѕРєСѓРјРµРЅС‚Р°С†РёСЏ](https://preactjs.com/)
- [Express РґРѕРєСѓРјРµРЅС‚Р°С†РёСЏ](https://expressjs.com/)
- [Supabase РґРѕРєСѓРјРµРЅС‚Р°С†РёСЏ](https://supabase.com/docs)
- [OpenAI API РґРѕРєСѓРјРµРЅС‚Р°С†РёСЏ](https://platform.openai.com/docs)
- [TypeScript РґРѕРєСѓРјРµРЅС‚Р°С†РёСЏ](https://www.typescriptlang.org/docs/)

## Р’РєР»Р°Рґ РІ РїСЂРѕРµРєС‚

1. РЎРѕР·РґР°Р№С‚Рµ РІРµС‚РєСѓ РґР»СЏ РІР°С€РµР№ С„СѓРЅРєС†РёРё
2. Р’РЅРµСЃРёС‚Рµ РёР·РјРµРЅРµРЅРёСЏ
3. РџСЂРѕС‚РµСЃС‚РёСЂСѓР№С‚Рµ Р»РѕРєР°Р»СЊРЅРѕ
4. РЎРѕР·РґР°Р№С‚Рµ Pull Request СЃ РѕРїРёСЃР°РЅРёРµРј РёР·РјРµРЅРµРЅРёР№

## Р’РѕРїСЂРѕСЃС‹ Рё РїРѕРґРґРµСЂР¶РєР°

Р•СЃР»Рё Сѓ РІР°СЃ РµСЃС‚СЊ РІРѕРїСЂРѕСЃС‹ РёР»Рё РїСЂРѕР±Р»РµРјС‹:
1. РџСЂРѕРІРµСЂСЊС‚Рµ РґРѕРєСѓРјРµРЅС‚Р°С†РёСЋ
2. РџРѕСЃРјРѕС‚СЂРёС‚Рµ СЃСѓС‰РµСЃС‚РІСѓСЋС‰РёРµ issues
3. РЎРѕР·РґР°Р№С‚Рµ РЅРѕРІС‹Р№ issue СЃ РѕРїРёСЃР°РЅРёРµРј РїСЂРѕР±Р»РµРјС‹
