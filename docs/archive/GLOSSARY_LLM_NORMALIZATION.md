---
stale: true
status: archived
domain: meta
---

# Р“Р»РѕСЃСЃР°СЂРёР№: РІС‹РІРѕРґ РЅРµР№СЂРѕСЃРµС‚Рё Рё РЅРѕСЂРјР°Р»РёР·Р°С†РёСЏ

## РћС‚РєСѓРґР° Р±РµСЂСѓС‚СЃСЏ РґР°РЅРЅС‹Рµ

- **РђРЅР°Р»РёР· (Stage 1)** вЂ” РјРѕРґРµР»СЊ РІРѕР·РІСЂР°С‰Р°РµС‚ JSON: `characters`, `locations`, `terms`, Р° С‚Р°РєР¶Рµ `updatedCharacters` / `updatedLocations` / `updatedTerms`.
- **API** вЂ” РїРѕР»СЊР·РѕРІР°С‚РµР»СЊ РјРѕР¶РµС‚ СЃРѕР·РґР°РІР°С‚СЊ/СЂРµРґР°РєС‚РёСЂРѕРІР°С‚СЊ Р·Р°РїРёСЃРё РіР»РѕСЃСЃР°СЂРёСЏ (РїРѕР»СЏ `type`, `gender`, `notes` Рё С‚.Рґ.).

РњРѕРґРµР»СЊ РЅРµ РіР°СЂР°РЅС‚РёСЂСѓРµС‚ СЃС‚СЂРѕРіРѕ Р·Р°РґР°РЅРЅС‹Р№ С„РѕСЂРјР°С‚: РІРѕР·РјРѕР¶РЅС‹ РґСЂСѓРіРёРµ СЂРµРіРёСЃС‚СЂС‹, СЃРёРЅРѕРЅРёРјС‹ (`masculine` РІРјРµСЃС‚Рѕ `male`), РѕРїРµС‡Р°С‚РєРё РёР»Рё РїСѓСЃС‚С‹Рµ Р·РЅР°С‡РµРЅРёСЏ. Р§С‚РѕР±С‹ РЅРµ РїРѕР»СѓС‡Р°С‚СЊ РѕС€РёР±РєРё РѕРіСЂР°РЅРёС‡РµРЅРёР№ Р‘Р” Рё РЅРµ Р»РѕРјР°С‚СЊ РїР°Р№РїР»Р°Р№РЅ, РІСЃРµ enum-РїРѕРґРѕР±РЅС‹Рµ РїРѕР»СЏ РЅРѕСЂРјР°Р»РёР·СѓСЋС‚СЃСЏ.

---

## Р§С‚Рѕ РјРѕР¶РµС‚ РІРµСЂРЅСѓС‚СЊ РЅРµР№СЂРѕСЃРµС‚СЊ (Р°РЅР°Р»РёР·Р°С‚РѕСЂ)

РџРѕ РїСЂРѕРјРїС‚Сѓ РІ `engine/prompts/system/analyzer.ts` РѕР¶РёРґР°РµС‚СЃСЏ:

| РЎСѓС‰РЅРѕСЃС‚СЊ   | РџРѕР»Рµ       | РћР¶РёРґР°РµРјС‹Рµ Р·РЅР°С‡РµРЅРёСЏ |
|-----------|------------|---------------------|
| characters | `gender`   | `male` \| `female` \| `neutral` \| `unknown` |
| characters | `role`     | `protagonist` \| `antagonist` \| `supporting` \| `minor` |
| locations  | `type`     | `city` \| `country` \| `building` \| `region` \| `world` \| `other` |
| terms      | `category` | `skill` \| `magic` \| `item` \| `title` \| `organization` \| `race` \| `other` |

РќР° РїСЂР°РєС‚РёРєРµ РјРѕРґРµР»СЊ РјРѕР¶РµС‚ РІРµСЂРЅСѓС‚СЊ, РЅР°РїСЂРёРјРµСЂ: `"masculine"`, `"f"`, `"Female"`, `"town"`, `"spell"`, РїСѓСЃС‚СѓСЋ СЃС‚СЂРѕРєСѓ РёР»Рё Р·РЅР°С‡РµРЅРёРµ РЅРµ РёР· СЃРїРёСЃРєР°.

---

## Р“РґРµ РЅРѕСЂРјР°Р»РёР·СѓРµРј

### 1. Stage 1 (Р°РЅР°Р»РёР·) вЂ” `engine/stages/stage-1-analyze.ts`

РџРµСЂРµРґ С‚РµРј РєР°Рє РґР°РЅРЅС‹Рµ СѓС…РѕРґСЏС‚ РІ РїР°Р№РїР»Р°Р№РЅ Рё РІ РіР»РѕСЃСЃР°СЂРёР№:

- **gender** в†’ `normalizeGender()` в†’ РІСЃРµРіРґР° РѕРґРЅРѕ РёР· `male` | `female` | `neutral` | `unknown`.
- **location type** в†’ `normalizeLocationType()` в†’ РѕРґРЅРѕ РёР· `city` | `country` | `building` | `region` | `world` | `other` (СЃРёРЅРѕРЅРёРјС‹ РІСЂРѕРґРµ `town`/`village` в†’ `city`, `area`/`zone` в†’ `region`).
- **term category** в†’ `normalizeTermCategory()` в†’ РѕРґРЅРѕ РёР· `skill` | `magic` | `item` | `title` | `organization` | `race` | `other` (РЅР°РїСЂРёРјРµСЂ, `spell`/`ability` в†’ `magic`).

РўР°Рє РїРѕ РїР°Р№РїР»Р°Р№РЅСѓ РёРґСѓС‚ С‚РѕР»СЊРєРѕ РґРѕРїСѓСЃС‚РёРјС‹Рµ Р·РЅР°С‡РµРЅРёСЏ, РґР°Р¶Рµ РµСЃР»Рё РјРѕРґРµР»СЊ РѕС‚РІРµС‚РёР»Р° РёРЅР°С‡Рµ.

### 2. Р—Р°РїРёСЃСЊ РІ Р‘Р” вЂ” `services/supabaseDatabase.ts`

РџРµСЂРµРґ РІСЃС‚Р°РІРєРѕР№/РѕР±РЅРѕРІР»РµРЅРёРµРј РІ С‚Р°Р±Р»РёС†Рµ `glossary_entries`:

- **gender** в†’ `normalizeGenderForDB()` в†’ РґРѕРїСѓСЃС‚РёРјРѕРµ Р·РЅР°С‡РµРЅРёРµ РёР»Рё `null` (РѕРіСЂР°РЅРёС‡РµРЅРёРµ `glossary_entries_gender_check`).
- **type** (РІРёРґ Р·Р°РїРёСЃРё) в†’ `normalizeGlossaryTypeForDB()` в†’ РІСЃРµРіРґР° `character` | `location` | `term` (РЅР° СЃР»СѓС‡Р°Р№ API РёР»Рё Р±СѓРґСѓС‰РёС… РїСѓС‚РµР№ СЃ РЅРµРєРѕСЂСЂРµРєС‚РЅС‹Рј Р·РЅР°С‡РµРЅРёРµРј).

РџРѕР»СЏ `notes` Рё `description` РІ Р‘Р” вЂ” СЃРІРѕР±РѕРґРЅС‹Р№ С‚РµРєСЃС‚, РѕРіСЂР°РЅРёС‡РµРЅРёР№ РїРѕ enum РЅРµС‚.

---

## РћРіСЂР°РЅРёС‡РµРЅРёСЏ Р‘Р” (Supabase)

- **glossary_entries.gender** вЂ” check constraint, РґРѕРїСѓСЃС‚РёРјС‹ С‚РѕР»СЊРєРѕ `male`, `female`, `neutral`, `unknown` РёР»Рё `NULL`.
- **glossary_entries.type** вЂ” С‚РёРї Р·Р°РїРёСЃРё: С‚РѕР»СЊРєРѕ `character`, `location`, `term`.

РћСЃС‚Р°Р»СЊРЅС‹Рµ РїРѕР»СЏ РіР»РѕСЃСЃР°СЂРёСЏ (original, translated, description, notes, declensions, first_appearance, mentioned_in_chapters, image_urls Рё С‚.Рґ.) РЅРµ РёРјРµСЋС‚ enum-РѕРіСЂР°РЅРёС‡РµРЅРёР№; РїСЂРё РЅРµРѕР±С…РѕРґРёРјРѕСЃС‚Рё РїСЂРѕРІРµСЂСЏСЋС‚СЃСЏ С‚РѕР»СЊРєРѕ РЅР° СѓСЂРѕРІРЅРµ РїСЂРёР»РѕР¶РµРЅРёСЏ (РЅР°РїСЂРёРјРµСЂ, РЅРµРїСѓСЃС‚С‹Рµ СЃС‚СЂРѕРєРё).

---

## РС‚РѕРі

| РСЃС‚РѕС‡РЅРёРє   | РџРѕР»Рµ / СЃСѓС‰РЅРѕСЃС‚СЊ | Р РёСЃРє Р±РµР· РЅРѕСЂРјР°Р»РёР·Р°С†РёРё        | Р“РґРµ РЅРѕСЂРјР°Р»РёР·СѓРµРј        |
|-----------|------------------|------------------------------|------------------------|
| LLM       | character.gender | РћС€РёР±РєР° `glossary_entries_gender_check` | stage-1 + Supabase     |
| LLM       | location.type    | РќРµРІРµСЂРЅС‹Р№ С‚РёРї РІ РїР°Р№РїР»Р°Р№РЅРµ     | stage-1                |
| LLM       | term.category    | РќРµРІРµСЂРЅР°СЏ РєР°С‚РµРіРѕСЂРёСЏ РІ РїР°Р№РїР»Р°Р№РЅРµ| stage-1                |
| API/РєРѕРґ   | entry.type       | РћС€РёР±РєР° РїСЂРё РІСЃС‚Р°РІРєРµ (РµСЃР»Рё РµСЃС‚СЊ type_check) | Supabase               |
| API/РєРѕРґ   | entry.gender     | РћС€РёР±РєР° `glossary_entries_gender_check` | Supabase               |

РўР°РєРёРј РѕР±СЂР°Р·РѕРј, Рё РІС‹РІРѕРґ РЅРµР№СЂРѕСЃРµС‚Рё, Рё РґР°РЅРЅС‹Рµ РёР· API РїСЂРёРІРѕРґСЏС‚СЃСЏ Рє РґРѕРїСѓСЃС‚РёРјС‹Рј Р·РЅР°С‡РµРЅРёСЏРј РґРѕ Р·Р°РїРёСЃРё РІ Р‘Р” Рё РїСЂРё РїРµСЂРµРґР°С‡Рµ РїРѕ РїР°Р№РїР»Р°Р№РЅСѓ.
