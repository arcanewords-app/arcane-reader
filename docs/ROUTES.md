# Routes (stub)

**Canonical route map:** [`.cursor/rules/routing.mdc`](../.cursor/rules/routing.mdc)

Edit routes in that file in the same PR as changes to:

- `src/client/AppRouter.tsx` (frontend)
- `src/server.ts` (API)

For human-readable docs and navigation flows, open the rule file in the repo or Obsidian via `docs/_canonical/rules/routing.mdc` (junction).

Do not treat this stub as the source of truth.
